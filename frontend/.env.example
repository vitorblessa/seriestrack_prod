# Copy to `.env` for local dev. On Vercel/Netlify, set this as a project
# environment variable instead (it gets baked into the build at build time,
# since this is a Create React App project).

REACT_APP_BACKEND_URL=https://seriestrack-backend.onrender.com
